<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8" />
        <title>Experimental most read articles upon REDIS</title>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
        <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">
    </head>
    <body>
      <div class="container">
        <div class="row">
          <div class="col-md-12">
            <h1>Experimental redis most read articles</h1>
            <p>Type any route, it will be added in redis most read articles</p>
            <p>Actual route is {{ actualRoute }}</p>
            <h2>Infos</h2>
            <p>TTL for most read articles is set to {{ttl}}</p>
            <h2>Most read articles</h2>
            {% if bestArticles|length > 0 %}
            <ul>
            {% for url, bestArticle in bestArticles %}
                <li>{{ url }} : {{ bestArticle }}</li>
            {% endfor %}
            </ul>
            {% endif %}
          </div>
      </div>
    </body>
</html>
